{# 
    Template addformation.html ~ Modification d'une formation via back-office
    @author Ethan MÉNAGÉ [ethanmng.pro@gmail.com]
#}
{% extends 'admin/baseback.html.twig' %}

{% block body %}
    <div class="container mt-4">
        <h1 class="text-primary text-center mb-4">Modifier la Formation</h1>

        <div class="card shadow-sm border-0 rounded-4 p-4 mb-4 bg-light">
            {{ include('admin/formations/_formulaire.html.twig', { 'button_label': 'Mettre à jour' }) }}
        </div>

        <div class="text-center">
            <a href="{{ path('admin.formations') }}" class="btn btn-outline-primary btn-lg" role="button">
                Retour à la liste des formations
            </a>
        </div>
    </div>
{% endblock %}